import 'package:flutter/material.dart';

class Question01 extends StatelessWidget {
  const Question01({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Scaffold(
        body: Text('Question 1'),
      ),
    );
  }
}
